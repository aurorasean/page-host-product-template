name: Register product-your-name-here in page-host

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  register:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout page-host
        uses: actions/checkout@v4
        with:
          repository: aurorasean/page-host
          token: ${{ secrets.PAT_TOKEN }}
          path: page-host

      - name: Check if product-your-name-here is already registered
        id: check
        run: |
          if grep -qE '^(staging|prod)/product-your-name-here$' page-host/products.txt; then
            echo "already_registered=true" >> "$GITHUB_OUTPUT"
          else
            echo "already_registered=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Create PR to register product-your-name-here
        if: steps.check.outputs.already_registered == 'false'
        env:
          GH_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          cd page-host
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          BRANCH="register-product-your-name-here"
          git checkout -b "$BRANCH"
          printf '\nstaging/product-your-name-here\nprod/product-your-name-here\n' >> products.txt
          git add products.txt
          git commit -m "Register product-your-name-here in products.txt"
          git push origin "$BRANCH"
          gh pr create \
            --repo aurorasean/page-host \
            --title "Register product-your-name-here in products.txt" \
            --body "Adds \`staging/product-your-name-here\` and \`prod/product-your-name-here\` to \`products.txt\` so that product-your-name-here appears on the page-host landing page." \
            --base main \
            --head "$BRANCH"
